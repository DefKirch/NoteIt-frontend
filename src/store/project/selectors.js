export const selectProject = (state) => state.project.project;
export const selectMyProjects = (state) => state.project.myProjects;
export const selectProjectId = (state) => state.project.project.id;
export const selectMe = (state) => state.user.profile;
